# TYPESCRYPT DECORATORS PRACTICE

This is compiles by webpack in a single file bundle.js

## Start

`yarn start` or `npm run start`

## Dev

`yarn dev`or `npm run dev`

## Build

`yarn build` or `npm run build`
